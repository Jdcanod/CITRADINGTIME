# Story 1 — Scaffold del proyecto

**Como** desarrollador, **quiero** una estructura base de archivos estáticos, **para que** el resto de historias tengan dónde implementarse.

## Criterios de aceptación
- [x] `index.html` con estructura semántica base (`header`, `main`, secciones con `id`, `footer`)
- [x] `css/styles.css` con reset básico, variables CSS de la paleta y tipografías cargadas
- [x] `js/main.js` vacío, enlazado con `defer`
- [x] `assets/logo.jpeg` disponible
- [x] Meta tags SEO básicos (title, description, favicon, Open Graph)
